Interview Questions and Answers Repository

Welcome to the Easy SQL Interview Questions repository! 🎯

This project is designed to serve as a comprehensive resource for commonly asked SQL interview questions, along with detailed answers and explanations. Whether you’re preparing for an interview, revisiting key concepts, or simply expanding your knowledge, this repository aims to be your go-to reference.

Why I Created This Repository

🌟 Personal Growth: To document and solidify my understanding of important topics.
🤝 Community Support: To help others by providing structured, easy-to-digest answers for popular interview questions.
📚 Resourcefulness: To create a reliable resource I (and others) can revisit whenever needed.

What You’ll Find Here

📂 Categories: Questions are organized into sections (e.g., Data Analytics, Coding, Behavioral, System Design).
✅ Detailed Answers: Each question is paired with clear, concise, and well-researched answers.
💡 Tips & Tricks: Where applicable, practical advice and interview strategies are included.

How to Use This Repository

Explore Topics: Browse the categorized questions to find areas relevant to your needs.
Study Answers: Learn the answers and grasp the underlying concepts.
Contribute: Spot an improvement or have additional insights? Feel free to open an issue or submit a pull request!

Feedback and Contributions

Your feedback is invaluable! If you have suggestions, corrections, or new questions to add, please don’t hesitate to contribute or reach out. 

Together, we can make this a robust and valuable resource for the community.

Questions & Answers

1- Write a query to select all records from a table called Customers.

SELECT * 
FROM Customers;

2- Write a query to fetch all orders from the Orders table where the OrderAmount is greater than 500.

SELECT * 
FROM Orders
WHERE OrderAmount > 500;

3- Write a query to fetch customer names and sort them alphabetically from the Customers table.

SELECT CustomerName 
FROM Customers
ORDER BY CustomerName ASC;

4- Write a query to fetch only the FirstName and LastName columns from the Employees table.

SELECT FirstName, 
       LastName 
FROM Employees;

5- Write a query to find unique cities from the Customers table.

SELECT DISTINCT City 
FROM Customers;

6- Write a query to count the total number of customers in the Customers table.

SELECT COUNT(*) AS TotalCustomers 
FROM Customers;

7- Write a query to calculate the total sales from the Sales table.

SELECT SUM(SalesAmount) AS TotalSales 
FROM Sales;

8- Write a query to group employees by their Department and count how many employees work in each department.

SELECT Department, 
       COUNT(*) AS EmployeeCount
FROM   Employees
GROUP BY Department;

9- Write a query to calculate the average salary of employees from the Employees table.

SELECT AVG(Salary) AS AverageSalary 
FROM Employees;

10- Write a query to find the highest order amount from the Orders table.

SELECT MAX(OrderAmount) AS HighestOrder
FROM Orders;

11- Write a query to fetch customer names and their respective order amounts by joining the Customers and Orders tables.

SELECT Customers.CustomerName,
       Orders.OrderAmount
FROM Customers
INNER JOIN Orders
ON Customers.CustomerID = Orders.CustomerID;

12- Write a query to fetch all customers and their order details, even if the customer has not placed an order.

SELECT Customers.CustomerName,
       Orders.OrderAmount
FROM Customers
LEFT JOIN Orders
ON Customers.CustomerID = Orders.CustomerID;

13- Write a query to fetch all orders and the customer details for those orders, even if the customer information is missing.

SELECT Orders.OrderID, 
       Customers.CustomerName
FROM Orders
RIGHT JOIN Customers
ON Customers.CustomerID = Orders.CustomerID;

14- Write a query to generate all possible combinations of rows between the Products and Categories tables.

SELECT Products.ProductName,
       Categories.CategoryName
FROM Products
CROSS JOIN Categories;

15- Write a query to fetch all employees whose DepartmentID is in (1, 2, 3).

SELECT EmployeeName 
FROM Employees
WHERE DepartmentID IN (1, 2, 3);

16- Write a query to fetch orders where the OrderDate is between '2023-01-01' and '2023-12-31'.

SELECT * 
FROM Orders
WHERE OrderDate BETWEEN '2023-01-01' AND '2023-12-31';

17- Write a query to find employees who earn more than the average salary in the Employees table.

SELECT EmployeeName 
FROM Employees
WHERE Salary > (SELECT AVG(Salary) FROM Employees);

18- Write a query to check if there are any orders from a specific customer with CustomerID = 10.

SELECT * 
FROM Orders
WHERE EXISTS (
    SELECT 1 
    FROM Customers 
    WHERE Customers.CustomerID = Orders.CustomerID AND Customers.CustomerID = 10);

19- Write a query to insert a new customer into the Customers table with Name = 'John Doe' and City = 'Seattle'.

INSERT INTO Customers (CustomerName, City)
VALUES ('John Doe', 'Seattle');

20- Write a query to update the OrderStatus to 'Shipped' for orders where OrderID = 100.

UPDATE Orders
SET OrderStatus = 'Shipped'
WHERE OrderID = 100;

21- Write a query to delete all records from the Customers table where the City is NULL.

DELETE FROM Customers
WHERE City IS NULL;

22- Write a query to create a table called Products with columns: ProductID, ProductName, Price.

CREATE TABLE Products (
    ProductID INT PRIMARY KEY,
    ProductName VARCHAR(100),
    Price DECIMAL(10, 2));

23- Write a query to delete the OldProducts table from the database.

DROP TABLE OldProducts;
